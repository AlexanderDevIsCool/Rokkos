<li>
  <%= link_to user.name, user %>
  <% if current_user.admin? && !current_user?(user) %>
    | <%= link_to "видалити", user, method: :delete,
                  data: { confirm: "Ви впевнені ?" } %>
  <% end %>
</li>